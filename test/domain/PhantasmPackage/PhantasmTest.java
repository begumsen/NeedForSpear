package domain.PhantasmPackage;

public class PhantasmTest {
	Phantasm paddle = Phantasm.getInstance();
	/*
	 * @Test public void testIsMovingLeftModified() { paddle.leftMovement();
	 * assertTrue(paddle.isMovingLeft()); }
	 * 
	 * @Test public void testIsMovingRightModified() { paddle.leftMovement();
	 * assertFalse(paddle.isMovingRight()); }
	 * 
	 * @Test public void testMovesFast() { paddle.setX(paddle.getDx()*3); // To
	 * ensure paddle can move by 2dx int oldXcoordinate = paddle.getX();
	 * paddle.setMovingLeft(true); paddle.leftMovement();
	 * assertTrue(paddle.getX()==(oldXcoordinate - (2*paddle.getDx()))); }
	 * 
	 * @Test public void testHitsBorder() { paddle.setX(paddle.getDx()/2); // to
	 * ensure paddle will not be able to move int oldXcoordinate = paddle.getX();
	 * paddle.leftMovement(); assertTrue(paddle.getX()==oldXcoordinate); }
	 * 
	 * @Test public void testHitsBorderWhileFast() { paddle.setMovingLeft(true);
	 * paddle.setX((3*paddle.getDx())/2); // to ensure paddle cannot move while
	 * fast. int oldXcoordinate = paddle.getX(); paddle.leftMovement();
	 * assertTrue(paddle.getX()==oldXcoordinate); }
	 */
}
